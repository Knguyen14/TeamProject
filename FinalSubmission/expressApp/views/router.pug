doctype html
html
  head
    meta(charset='utf-8')
    title
    meta(name='viewport', content='initial-scale=1,maximum-scale=1,user-scalable=no')
    script(src='https://api.mapbox.com/mapbox-gl-js/v0.36.0/mapbox-gl.js')
    link(href='https://api.mapbox.com/mapbox-gl-js/v0.36.0/mapbox-gl.css', rel='stylesheet')
    style.
      body { margin:0; padding:0; }
      #map { position:absolute; top:0; bottom:0; width:100%; }
  body
    script(src='https://api.mapbox.com/mapbox-gl-js/plugins/mapbox-gl-directions/v3.1.1/mapbox-gl-directions.js')
    link(rel='stylesheet', href='https://api.mapbox.com/mapbox-gl-js/plugins/mapbox-gl-directions/v3.1.1/mapbox-gl-directions.css', type='text/css')
    #map
    script.
      mapboxgl.accessToken = 'pk.eyJ1IjoidmlrYTUxODYiLCJhIjoiY2l6cnQ0aHNqMDBjdTMycGNneTdvaWN3bSJ9.QPqOCcSpyB3Ef4NO9Eg8BA';
      var map = new mapboxgl.Map({
      container: 'map',
      style: 'mapbox://styles/mapbox/streets-v9', //stylesheet location
      center: [-105.2659, 40.0076], // starting position
      zoom: 15 // starting zoom
      });
      //Locate the user
      var geoLocate = new mapboxgl.GeolocateControl();
      map.addControl(geoLocate);
      geoLocate.on('geolocate', function(e) {
      console.log(e);
      //map.setZoom(15)
      });
      //Add buttons to zoom into current location
      var navigate = new mapboxgl.NavigationControl();
      map.addControl(navigate);
      //Add Walking Directions
      var directions = new MapboxDirections({
      accessToken: mapboxgl.accessToken,
      profile: 'walking'
      //container: 'directions'
      });
      map.addControl(directions, 'top-left');
      //Trying to add current location as starting location
      //directions.setOrigin('geoLocate');

